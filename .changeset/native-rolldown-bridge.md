---
"@rsvelte/vite-plugin-svelte": minor
---

Use the native `@rsvelte/rolldown` compiler for compatible client and server production builds while preserving the existing Vite plugin interface and safe fallbacks, including SvelteKit configurations that retain unset compiler options.
